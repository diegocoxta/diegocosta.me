export const usePathname = jest.fn().mockImplementation(() => 'https://ciclik.com.br');

export const notFound = jest.fn().mockImplementation(() => 'NOT-FOUND');

export const redirect = jest.fn().mockImplementation();
